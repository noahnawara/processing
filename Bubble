void setup() {
  size(500, 500);
  background(44,62,80);
}

void draw() {
    background(44,62,80);
  float mX = map(mouseX, 0, width, 0, 500);
  float mY = map(mouseY, 0, width, 0, 500);
  fill(HSB,80+mY,60+mX,mY);
  noStroke();
  ellipse(mX, mY, mY, mY);
}
